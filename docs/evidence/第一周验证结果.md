# 第一周验证结果

执行日期：2026-09-06。环境：Windows，项目 WAF Python 环境，Vue CLI / Jest。

| 检查 | 结果 |
| --- | --- |
| 后端 RBAC / MFA / 审计集成与算法测试 | 12 组通过；原始输出见 first-week-results.txt |
| 前端已有校验与安全工具单元测试 | 2 套测试、20 项通过 |
| 前端生产构建 | 成功；有入口包体积建议警告（约 274 KiB，未压缩） |
| 真实浏览器交互与视觉检查 | 本机 Chrome 五条主要流程通过，无页面脚本错误；桌面与390px移动端已检查 |
| 本机 MySQL 初始化 | 新增安全表、五个第一周演示账号及两个商户商品创建成功 |
| 真实 MySQL 并发测试 | 未执行；自动测试使用独立 SQLite 内存数据库 |

后端测试覆盖：管理员和审计员强制二因素验证、首次绑定恢复码、重复挑战拒绝、TOTP 时间偏差/重放/锁定、恢复码一次性、换绑及会话吊销、账号角色变更、普通用户越权、商户跨店修改、订单跨用户访问、管理员管理校验、重复发货拒绝、审计导出及审计员自审计、公钥证书文件登录拒绝、伪造 mTLS 头拒绝、演示短信不能重置特权账号密码、无 Redis 时退出令牌失效。

浏览器原始结果为 `browser-results.json`，截图为 `admin-overview.png`、`admin-products.png`、`admin-audit.png`、`admin-mobile.png`。浏览器操作使用独立内存测试数据库，不涉及真实用户数据或支付。审计员/商户的越权拒绝及恢复因素等场景由后端测试覆盖。

第一阶段以外的国密信封、安全扫描、依赖扫描、JMeter 压测未纳入本次交付，也未宣称其验收通过。
